@page "/"
@using WebApp.Components.Boards

<div class="container-fluid">
    <h3 class="text-center fw-semibold mb-4">My Boards</h3>

    <div class="row">
        <div class="col-lg-9 col-md-8 order-2 order-md-1">
            <div class="row g-3">
                @foreach (var board in Boards)
                {
                <BoardCard Board="@board" />
                }
            </div>
        </div>

        <div class="col-lg-3 col-md-4 order-1 order-md-2 mb-3 mb-md-0">
            <div class="card filter-panel">
                <EditForm Model="@filter" class="card-body">
                    <h6 class="fw-semibold mb-3">Filters</h6>

                    <InputText class="form-control mb-3"
                               placeholder="Board name"
                               @bind-Value="filter.Title"
                               @bind:event="oninput" />

                    <div class="form-check">
                        <InputCheckbox class="form-check-input"
                                       id="myBoards"
                                       @bind-Value="filter.OnlyMyBoards" />
                        <label class="form-check-label" for="myBoards">
                            Only my boards
                        </label>
                    </div>
                </EditForm>
            </div>
        </div>
    </div>
</div>